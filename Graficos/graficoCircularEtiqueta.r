# Create a vector of pies
x <- c(10, 20, 30, 40)

# Create a vector of labels
etiqueta <- c("Manzanas", "Bananas", "Frambuesas", "Peras")

# Display the pie chart with labels
pie(x, label = etiqueta, main = "Frutas")
